{
  "name": "apetimism",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ ntchjb/apetimism",
    "create-local": "graph create --node http://localhost:8020/ ntchjb/apetimism",
    "remove-local": "graph remove --node http://localhost:8020/ ntchjb/apetimism",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ntchjb/apetimism"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.3",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
